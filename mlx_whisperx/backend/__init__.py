"""Internal ASR backends used by mlx-whisperx.

The package currently vendors an MLX Whisper implementation so the outer pipeline can
run ASR locally on Apple Silicon without depending on a separately installed backend.
"""
